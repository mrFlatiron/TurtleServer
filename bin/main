#!/usr/bin/perl
use utf8;
use strict;
use warnings;

BEGIN {
  my $path = __FILE__;
  $path =~ s{/[^/]+$}{};
  chdir($path);
}


use TurtleLib;
use Turtle::Server;
binmode STDOUT, ":utf8";
binmode STDERR, ":utf8";

my $server = Turtle::Server->new;

$server->start;
